# Ubuntu Orange Edition — GRUB theme
#
# Orange boot menu with near-white entries and a deep-orange selection highlight.
# Colors match ../../../theme/orange-edition.css:
#   surface #e95420, text #ffffff, muted #ffd9c9, edge #c7431a, accent #a8371a
#
# GRUB themes use named colors / hex strings. Fonts must be supplied as .pf2
# and referenced by their internal name; the installer converts a TTF at build
# time (grub-mkfont) and rewrites the font names if a different face is used.
# The defaults below assume DejaVu Sans, which is present on Ubuntu build hosts.

# ---- Global ----
title-text: ""
desktop-color: "#e95420"
desktop-image: ""
message-color: "#ffffff"
message-bg-color: "#e95420"
terminal-box: "terminal_box_*.png"
terminal-font: "Unifont Regular 16"

# ---- Boot menu title ----
+ label {
  top          = 8%
  left         = 0
  width        = 100%
  height       = 40
  align        = center
  color        = "#ffffff"
  font         = "DejaVu Sans Bold 20"
  text         = "Ubuntu Orange Edition"
}

+ label {
  top          = 8%+34
  left         = 0
  width        = 100%
  height       = 24
  align        = center
  color        = "#ffd9c9"
  font         = "DejaVu Sans Regular 12"
  text         = "Ubuntu Determinant"
}

# ---- Boot menu (the selectable entries) ----
+ boot_menu {
  left            = 25%
  width           = 50%
  top             = 26%
  height          = 52%

  item_color              = "#ffffff"
  selected_item_color     = "#ffffff"
  item_font               = "DejaVu Sans Regular 16"
  selected_item_font      = "DejaVu Sans Bold 16"
  item_height             = 40
  item_padding            = 6
  item_spacing            = 6
  icon_width              = 0
  icon_height             = 0

  # Orange row backgrounds; deep-orange highlight on the selected entry.
  # These 9-slice PNGs are generated by the installer from the token colors
  # when they are absent, so the theme renders even on a fresh build host.
  item_pixmap_style          = "item_*.png"
  selected_item_pixmap_style = "selected_item_*.png"

  scrollbar               = true
  scrollbar_width         = 6
  scrollbar_thumb         = "scrollbar_thumb_*.png"
}

# ---- Countdown / progress ----
+ progress_bar {
  id           = "__timeout__"
  left         = 25%
  top          = 82%
  width        = 50%
  height       = 6
  show_text    = false
  bar_style    = "progress_bg_*.png"
  highlight_style = "progress_fg_*.png"
  fg_color     = "#a8371a"
  bg_color     = "#c7431a"
}

# ---- Footer hint ----
+ label {
  top          = 90%
  left         = 0
  width        = 100%
  height       = 20
  align        = center
  color        = "#ffd9c9"
  font         = "DejaVu Sans Regular 11"
  text         = "Use the arrow keys to select. Press Enter to boot."
}
